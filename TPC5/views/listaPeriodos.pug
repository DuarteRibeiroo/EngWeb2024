extends layout

block content
  .w3-card-4
    header.w3-container.w3-pink
      h1= title
    .w3-container 
      table.w3-table-all
        thead
          tr
            th Id
            th Período
        tbody
          each periodo in lista
            tr
              td
                a(href= '/periodos/' + periodo.id)= periodo.id
              td= periodo.nome
              td= periodo.periodo
              td
                a.w3-button.w3-blue(href='/periodos/edit/' + periodo.id) Editar 
                a.w3-button.w3-red(href='/periodos/delete/' + periodo.id) Apagar
    footer.w3-footer.w3-pink
      address Gerado por EngWeb2024 em #{data}